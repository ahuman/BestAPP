有新软件接入后，执行以下代码：

```
git add .
git commit -m "说明"
git push-all
```

> 已经利用如下代码，设置"push-all"为同时上传github、gitee、gitcode:
>
> ```
> git config --global alias.push-all '!f() { git push origin main && git push gitee main && git push gitcode main; }; f'
> ```